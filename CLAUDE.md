# Pegasus — Élevage Scalbert - Les Marchais

## Quoi
PWA mobile-first de gestion équestre (santé, soins, finances, généalogie, groom, quiz) pour un usage familial privé. 7 chevaux actifs : Échalote (Échalote du Val d'Été), Hakéa (Hakea du Val d'Été), Romarin (Roi d'Ys), Cerise (Cervoise de Champfort), Fraise (Faveur de Champfort), Pistache (Pistache des Marchais), Pamplemousse (Pamplemousse du Val).

## Qui / Pourquoi
Propriétaire : Jean-Max Scalbert, CFO de formation, **sans compétences en codage**. Toute décision de code doit rester pilotable par lui : livraisons complètes, pas de fragments à chercher-remplacer, explications minimales sauf demande explicite. Logique CFO : traçabilité, coût par cheval, réconciliation.

Deux rôles utilisateurs : Famille (accès complet) et Groom (lecture seule bobos actifs, identité chevaux, calendrier — **aucun accès finances**).

## Stack
React + TypeScript + Tailwind CSS + Vite (PWA) · Supabase (PostgreSQL + Auth + RLS + Storage) · GitHub `jms200240/pegasus-marchais` · Vercel (CI/CD auto depuis main).

## Règles absolues (à ne jamais casser)

- **Schema Supabase = source de vérité.** Toujours vérifier `information_schema.columns` avant d'écrire une requête ou un composant. Noms réels : `born_at`, `pere_name`, `mere_name`, `note`, `opened_at`, `pere_id`, `mere_id` (jamais les équivalents anglais génériques).
- **package.json** : ne jamais éditer via un éditeur rich text/Word (quotes typographiques → JSON cassé).
- **BottomNav** : toute overlay plein écran doit avoir un padding bas ≥ `calc(1rem + 64px + env(safe-area-inset-bottom))`.
- **Photos vétérinaires** : bundlées comme assets Vite statiques (pas Supabase Storage).
- **Vaccinations** : rappels calculés en temps réel depuis l'historique brut (pas pré-calculés) ; exclusions permanentes. `vaccinations.next_reminder_override` (nullable) permet de surcharger la date calculée sans casser ce principe — si absente, le calcul reste 100% dynamique.
- **Contexte visite pro** : stocké en suffixe texte dans `note` (pas de colonne dédiée sur `health_event_visits`).
- **UNRESOLVED_IMPORT** sur une image = toujours un fichier manquant/non commité, jamais un bug logique.
- **Icônes manquantes** : sourcer depuis Tabler Icons ou dériver mathématiquement, pas de dessin main levée.
- **Finances — HT/taux TVA** : jamais écrits en base, affichés uniquement pour vérification contre la facture papier (par ligne, calcul HT↔TTC dans les deux sens selon le mode de saisie choisi) ; seul le TTC est stocké — au niveau ligne (arrondi 2 décimales) et au niveau facture (`invoices.total_ttc` = montant total saisi par l'utilisateur depuis la facture papier, pas recalculé depuis la somme des lignes).
- **Finances — répartition par cheval** : `equalSplit` (%) n'a aucune règle d'allocation du reliquat d'arrondi — chaque part est arrondie indépendamment, la somme peut donc dévier de quelques centimes de 100% (ex. 7 chevaux à 14.29% = 100.03%). `effectiveShares` (financeUtils.ts) calcule alors la part idéale du **dernier cheval sélectionné pour la ligne** pour ramener la somme à exactement 100%, et l'applique si l'écart monétaire correspondant pour le TTC de la ligne reste sous `SHARE_ROUNDING_TOLERANCE` (10 centimes) — au-delà, la ventilation reste bloquée telle quelle ("Somme : X% — doit être exactement 100%"). Quand l'ajustement s'applique (SaisieFacture.tsx) : le % affiché du dernier cheval est corrigé (ex. Pamplemousse 14.26% au lieu de 14.29%), "Somme : 100%" reste vert, et un message orange léger indique "Arrondi : X cents alloués à <cheval>". `splitTtcByShares` (conversion € par cheval, écriture finale des `expenses`) applique `effectiveShares` en amont — jamais sur la part "Autre" (`OTHER_KEY`).
- **intervenant_type** (`invoices`/`expenses`) réutilise l'énumération de `HealthEvent.type` (`veterinaire`/`marechal`/`dentiste`/`osteo`/`groom`) — pas de nouvelle nomenclature.
- **health_events.source** : colonne texte libre (Famille/Groom/Vétérinaire/etc.) traçant qui a enregistré l'entrée — pas de contrainte stricte, ouverte pour usages futurs.

## Workflow de livraison (à respecter strictement)

- Une étape à la fois, fichier complet, attente du "ok" avant l'étape suivante — sauf en Claude Code où l'édition directe des fichiers remplace le copier-coller (mais garder le principe : petites étapes validées, pas de gros commit monolithique surprise).
- Chaque instruction d'action commence par l'outil concerné.
- Vérifier le schema Supabase avant tout code touchant les données.
- Style de communication : direct, concis, en français. Pas d'explications ligne à ligne non demandées.
- Décisions d'architecture : recommander directement la meilleure option plutôt que présenter des alternatives à arbitrer.

## État actuel (à mettre à jour à chaque sprint)

Sprints livrés : S1 (fondation), S2 (module Chevaux), S3 (Soins/Bobos — BoboWizard 5 étapes, BoboCard journal), S4 (route /visite — VisiteSheet, galerie photos filtrée par tags), S5 partiel (module Finances — menu Saisie de facture / Suivi des coûts, ventilation multi-chevaux ou 1 seul cheval, bar charts drill-down cheval↔prestataire, fiche facture consultable/modifiable ; pipeline OCR/staging non démarré).

Opérationnel en production (`pegasus-marchais.vercel.app`) :
- VeterinairePicker (10 vétérinaires, photos statiques bundlées, rang 1–10)
- VisiteProSheet (sélecteur métier : Vétérinaire, Maréchal-ferrant, Ostéopathe, Dentiste)
- VaccineReminders (regroupement collapsible, exclusions permanentes, calcul temps réel)
- 121 lignes d'historique de vaccination importées en Supabase
- Finances : Saisie de facture (multi-chevaux ou 1 seul cheval, bucket "Autre" hors suivi) + Suivi des coûts (total annuel, ventilation cheval/prestataire en bar charts avec drill-down, factures via InvoiceDetailSheet — ouverture en lecture seule, bouton "Modifier" pour éditer)
- VisiteProSheet — Maréchal-ferrant : MarechalPicker (2 maréchaux, photos bundlées), bobos actifs filtrés sur pathologies pied/fourbure (Seime, Abcès du pied, Fourbure, Pourriture de fourchette, Fourmilière), flux "Soin maréchal" en cascade (Parage 4 pieds → Ferrure antérieurs → Ferrure 4 pieds) écrivant dans `health_events`
- VisiteProSheet — Ostéopathe : OsteopathePicker (1 ostéopathe, Maëlys Bizet, photo bundlée), pas de section bobos, flux "Soin ostéopathe" en cascade (Séance complète → Palpation simple). `validateSoinStep`/le suffixe de note sont désormais génériques par métier (marechal/osteo/veterinaire), plus câblés en dur sur le maréchal
- Fiches Vaccins (contenu IFCE, 4 vaccins en accordéon) accessible depuis Rappels vaccins
- Échelle de gravité en dégradé couleur (vert→rouge, remplace les étoiles), lecture seule et sélection interactive
- BoboWizard — Localisation : seule "Zone" est obligatoire ; Membre concerné/Région/Face (zone Membre) ou Côté (autres zones) sont des sélections indépendantes et facultatives (`pathology.has_laterality` n'est plus utilisé pour bloquer la progression)
- Finances : menu réordonné (Suivi des coûts en premier), bloc "Total TTC payé" visible directement dans le menu
- Finances.tsx : le total du menu se rafraîchit à chaque retour sur la vue 'menu' (dépend de `view`, plus un fetch unique au montage) — corrige un total figé après un aller-retour vers Saisie de facture/Suivi des coûts sans changer d'onglet (le composant Finances ne démonte pas entre les sous-vues)
- Saisie de facture : refonte complète — champ "Montant total de la facture TTC" (référence papier) juste après le bloc Ventilation, devenu la source de vérité pour `invoices.total_ttc` (plus une somme recalculée des lignes). Par ligne : Montant HT (gauche) et Montant TTC (droite) sont tous deux éditables simultanément — le dernier champ édité fixe `amountMode` (source du calcul), l'autre se recalcule via la TVA (`lineTtc`/`lineHtDisplay`). Pas de toggle HT/TTC. La ventilation par cheval (sélection + %) est factorisée dans `LineVentilation`.
- Saisie de facture — validation explicite en cascade (`InvoiceLineDraft.validated`/`autoSynced`/`amountValidated`) : le champ "Montant total de la facture TTC" est raccourci avec un bouton "Valider le montant" sur la même ligne — la section Lignes reste masquée tant qu'il n'est pas cliqué (toute réédition du montant après validation ré-invalide et remasque la section). Une fois validé, la Ligne 1 apparaît en mode TTC, préremplie avec le montant total (modifiable). Dans chaque ligne, sous la TVA, un bouton "Valider le montant" par ligne (même comportement que celui du total : verrouille l'état visuellement en "Montant validé", toute réédition HT/TTC/TVA le réinvalide) est un prérequis à `isLineValid`, condition du bouton "Valider la ligne" en bas de carte. Valider une ligne la verrouille entièrement (champs désactivés, badge "validée") ; seules les lignes validées comptent dans le solde (`fixedSum`) et sont soumises à l'enregistrement final. Tant que le solde restant (montant total − `fixedSum`) est positif, une nouvelle ligne en attente apparaît automatiquement en fin de liste (montant TTC prérempli = solde, libellé "· reste à ventiler", `autoSynced: true` tant qu'elle n'a pas été éditée à la main). "Valider la facture" reste bloqué tant qu'une ligne en attente subsiste ou en cas de dépassement du montant total.
- VaccinSheet : aperçu "Prochain rappel" groupé par date de cadence (ex. Grippe+Tétanos+Rage vs Rhino), date modifiable → `next_reminder_override`
- Galerie : suppression de photo (Famille), partage/téléchargement corrigés (l'ancien téléchargement ignorait l'attribut `download` sur URL cross-origin), grille en miniatures + lazy-loading avec repli sur la photo pleine taille si pas de miniature
- VisiteSheet/VisiteProSheet : génération de miniature (`thumbnail_url`) à l'upload d'une photo d'ambiance, best-effort (n'empêche jamais l'upload de l'original en cas d'échec) — même pattern que la Galerie
- Fiches Chevaux (liste + fiche détail) : vignette photo réelle (`src/lib/horsePhotos.ts`, assets `src/assets/chevaux/`) à la place de l'initiale, avec repli sur `photo_url` puis initiale si aucune photo bundlée — mécanisme purement basé sur `horse.name` (clé exacte, accents compris), fonctionne identiquement pour la cavalerie historique (`is_active=false`), y compris les cas d'homonymie où l'historique porte un suffixe distinctif en base (ex. `Cerise (1977)`, différent de l'actif `Cerise`) — bien vérifier lequel avant d'assigner une photo. Photos actuelles : Échalote, Hakéa, Romarin, Cerise, Fraise, Pistache, Pamplemousse (les 7 actifs, tous couverts) + Pomme, Haschich, Kwetsche, Lichen, Cerise (1977) (historique) ; le fichier `cerise.jpg` d'origine était en réalité celui de Cerise (1977) — renommé `cerise-1977.jpg`, un nouveau `cerise.jpg` fourni ensuite pour l'actif
- Soins → Rappels vaccins : liste détaillée triée par échéance croissante ("Vaccin(s) — chevaux concernés — avant le [date]", regroupe toutes les échéances connues même hors fenêtre d'alerte), date modifiable inline via `<input type="date">` gaté par une popup "Date validée avec le vétérinaire ?" avant écriture de `next_reminder_override`
- Accueil : photo d'ambiance tirée au hasard à chaque ouverture (plus la dernière uploadée), bouton "Démarrer une visite pro" repositionné sous la photo
- VisiteProSheet — Dentiste : DentistePicker (Loïc Hensmans, photo bundlée `src/assets/dentistes/`), flux "Soin dentiste" en cascade (Nivellement → Extraction), remplace le placeholder — table `dentistes` créée et confirmée
- VisiteProSheet — Vétérinaire : bouton "Soin véto" ouvre `SoinVetoSheet` (horodatage, soin en texte libre, chevaux concernés en tags colorés, commentaire, case "Rappel" optionnelle → date + texte, écrit dans `health_events` + `soin_reminders` si rappel coché) — table `soin_reminders` créée et confirmée
- Soins.tsx : nouvelle section "Rappels soins" (`SoinReminders`), affiche les rappels dont l'échéance est à ≤ 7 jours (ou dépassée), triés par date
- FicheCheval : "Historique médical" éclaté en 3 sections pliables avec compteur à droite du titre (Vaccins / Soins / Bobos, repliées par défaut) :
  - **Vaccins** : prochains rappels connus pour ce cheval + lien "Historique complet" ouvrant `VaccinHistorySheet` (ordre antéchronologique, filtres Tous/Grippe/Tétanos/Rhinopneumonie/Rage)
  - **Soins** (Véto/Maréchal/Ostéo/Dentiste) : `health_events` avec `pathology_id` null ET `type` dans l'énumération métier — exclut les bobos "Autre" à note libre (BoboWizard) qui n'ont pas de `type` métier
  - **Bobos** : le reste (liés à une pathologie, ou notes libres "Autre" sans type métier)
- FicheCheval : en-tête photo/nom en layout côte-à-côte (photo agrandie à gauche, nom/race/badge à droite) au lieu d'empilé centré — cadre coloré d'en-tête conservé à taille quasi identique (~1/3 écran) ; nom en police réduite au-delà de 10 caractères pour éviter la troncature
- VaccinHistorySheet : quand un vaccin précis est filtré (pas "Tous"), le nom du vaccin n'est plus répété sur chaque ligne (déjà induit par le filtre) — seulement Dr / lieu (normal) / date (gras)
- Généalogie complète : 29 individus en base (7 actifs + 22 "cavalerie historique", `horses.is_active=false`), importés depuis l'organigramme/fichier généalogique fournis par l'utilisateur. Chaîne multi-génération résolue via `pere_id`/`mere_id`/`pdm_id` chaque fois que l'ancêtre est aussi dans notre cavalerie (ex. Cerise 1977 → Pomme → Kwetsche → Échalote/Hakéa/Pamplemousse), sinon texte libre (ancêtres externes). Liens IFCE réels sur les 15 individus qui en ont une. `types.ts` `Horse`/`Genealogy` alignés sur le schéma réel (colonnes `sex`, `died_at`, `naisseur`, `adresse_elevage`, `generation`, `pdm_id`, `pere_url`/`mere_url`/`pdm_url` existaient déjà en base mais absentes du type)
- `GenealogyTreeSheet` (nouveau, `src/lib/genealogyLayout.ts`) : arbre généalogique visuel natif — boîtes (nom / sexe / année de naissance-décès) + connecteurs SVG. **Disposition fixe** (`MASTER_POSITIONS`, table rangée/colonne par cheval) reprenant telles quelles les positions relatives de l'organigramme HTML source, vérifiée à la main pour qu'aucun connecteur ne passe au niveau d'un cheval étranger à la relation (un placement automatique — barycentre, DFS — ne peut pas deviner ce genre de contrainte visuelle ; testé et rejeté par l'utilisateur après un premier essai avec croisements). Hyacinthe est alignée sur la rangée de Haricot (son co-parent pour Lichen/Muscade) plutôt que sur sa profondeur de lignée stricte ; Epinard II passe en demi-rangée pour lui laisser la place. Règles de tracé : ligne verticale simple si un seul parent est dans la cavalerie ; "T" horizontal (bus routé au milieu de l'intervalle entre rangées, jamais sur le bord des boîtes) dès que les deux parents sont internes ; éventail ("rateau") si plusieurs enfants partagent la même ancre. Mode ciblé (`focusHorseId`) : restreint l'arbre au cheval sélectionné + ses ascendants/descendants directs (`collectBloodline`), en réutilisant la même table de positions filtrée au sous-ensemble. Accessible via bouton "Arbre généalogique" dans Chevaux.tsx (sous la Cavalerie active) et via "Voir dans l'arbre généalogique" depuis chaque Fiche cheval (mode ciblé)
- Correctif généalogie : Chataigne et Cassis sont bien filles de Pomme (le fichier xlsx source avait leurs cellules Père/Mère vides ; confirmé via les lignes de connexion de l'organigramme HTML fourni) — `genealogy.mere_id`/`mere_name` corrigés. Haschich n'a aucun descendant. Relecture complète de l'organigramme HTML (notes explicites "X × Y NOM_ENFANT") pour vérifier toutes les relations — source de vérité en cas de doute, même quand ce n'est pas joli visuellement.
- Chevaux.tsx : "Cavalerie historique" dépliable, **ouverte par défaut** (`historicalOpen` initialisé à `true`), même présentation `HorseCard` que la Cavalerie active ; bouton "Arbre généalogique" avec contour vert (`border-2 border-primary`) pour le distinguer visuellement
- HorseCard (Chevaux.tsx) : le badge rouge (nombre de bobos actifs) est conservé, mais la jauge de gravité (`SeverityScale`) précédemment affichée sous le nom a été retirée — plus aucune indication de gravité sur la vignette liste, seulement sur la fiche détail du cheval
- FicheCheval : ascendants (Père/Mère/Père de mère) ET nouvelle section "Descendance" (résolution inverse) sont cliquables vers la fiche correspondante quand elle existe. Sections Vaccins/Soins/Bobos masquées pour la cavalerie historique (`!horse.is_active`, pas de données) — Identité/Généalogie/lien vers l'arbre conservés pour tous — bouton "Voir dans l'arbre généalogique" avec le même contour vert (`border-2 border-primary`) que le bouton équivalent de Chevaux.tsx
- VaccineReminders : quand "Vaccins à jour" (aucun rappel actif), affiche sous le message la date du dernier vaccin administré par type (Grippe/Tétanos/Rhinopneumonie/Rage), triée antéchronologique
- Soins.tsx : nouvelle section "Dernières visites pro" (`DernieresVisitesPro.tsx`, après Rappels vaccins/Rappels soins) — dernière date `health_events.opened_at` par métier (Vétérinaire/Maréchal-ferrant/Ostéopathe/Dentiste), "Aucune visite enregistrée" si aucune ligne pour ce type
- Galerie : toutes les vignettes affichées ensemble (grid 3 colonnes, antéchronologique) — les en-têtes de date par jour ont été retirés
- Soins.tsx : section "Voir les fiches maladies" en bas de page, ouvre `PathologyBrowserSheet` (recherche + nuage de mots, identique à l'étape 2 de BoboWizard) ; clic sur une pathologie ou validation de la recherche avec un résultat unique ouvre `FichePathologie` (réutilisée depuis BoboWizard.tsx)
- **Rôle Admin — activé en production.** 4ᵉ valeur de l'enum `user_role` (`'admin'`, ajoutée via `ALTER TYPE`). Bouton roue crantée (`Settings`, lucide-react) dans l'en-tête Accueil à côté de "Quitter", visible uniquement si `role==='admin'` — ouvre `AdminSettings.tsx` (menu de commandes, remplace le contenu de `<main>` tout en gardant BottomNav/en-tête, réinitialisé comme `selectedHorseId` au changement d'onglet). Première commande : `GestionAcces.tsx` — liste tous les comptes (email/nom/rôle) et permet d'attribuer un rôle à un compte déjà créé à partir de son adresse email. Repose sur deux fonctions Postgres `SECURITY DEFINER` (`public.admin_list_users()`, `public.admin_set_user_role(target_email, new_role)`) qui vérifient elles-mêmes `role='admin'` pour l'appelant (pas de policy RLS classique, pas de clé service_role côté client) ; l'email vient uniquement d'un `JOIN auth.users` à l'intérieur de la fonction, jamais dupliqué dans `public.users` — donc aucun risque qu'un rôle non-admin y accède via une requête brute. Flux d'attribution : la personne doit d'abord créer elle-même son compte (écran de connexion standard), l'admin lui attribue ensuite un rôle par email — pas de création de compte depuis l'app (aurait nécessité une Edge Function avec clé service_role, jugé disproportionné pour ce besoin).
- **Gestion des accès — enrichie** : chaque compte de la liste a une icône crayon (pré-remplit le formulaire d'attribution de rôle avec son email/rôle actuel) et une icône révoquer/réactiver l'accès (`admin_revoke_access`/`admin_restore_access`, fonctions `SECURITY DEFINER` qui posent `auth.users.banned_until` à une date lointaine ou `null`, confirmation avant action, impossible sur son propre compte — protection à la fois côté UI et côté fonction SQL). Nouvelle section "Créer un compte manuellement" : admin saisit email + mot de passe (généré via un bouton dé, ou personnalisé) + rôle, appelle `api/admin-create-user.ts` (fonction serverless Vercel, seule à utiliser la clé `service_role` Supabase — jamais exposée au client) qui vérifie d'abord que l'appelant est bien admin (revalide son JWT + relit son rôle via un client `service_role` côté serveur, pas de secret partagé statique comme pour `notify-new-user`), crée le compte avec `email_confirm: true` (connexion immédiate possible, pas d'email de confirmation à attendre — contourne aussi la limite d'envoi Supabase par défaut) et applique le rôle choisi. Les identifiants générés s'affichent une seule fois à l'écran pour que l'admin les transmette lui-même (SMS/oral/WhatsApp). Comptes existants triés actifs d'abord, révoqués à la fin ; le clic sur "Modifier le rôle" fait défiler jusqu'au formulaire (`scrollIntoView`) et affiche un bandeau "Modification de &lt;email&gt;" — sans ça, le formulaire se pré-remplissait hors de vue en bas de page et l'action passait inaperçue. Pas de suppression de compte depuis l'app (géré manuellement par l'admin via le dashboard Supabase si besoin). Titre "Attribuer un compte" sur ce formulaire ; `admin_list_users()` renvoie aussi `last_sign_in_at` (`auth.users`), affiché par compte ("Jamais" si aucune connexion) ; tri secondaire par rôle au sein des actifs comme des révoqués, du plus ouvert au plus fermé (Admin > Famille > Groom > Visiteur).
- **Changer mon mot de passe** : icône clé dans l'en-tête Accueil, visible pour **tout rôle** (pas seulement admin) — `ChangePasswordModal.tsx`, `supabase.auth.updateUser({ password })`. Ne dépend d'aucun envoi d'email (contrairement à un vrai "mot de passe oublié", non implémenté) : fonctionne uniquement pour un utilisateur déjà connecté qui veut remplacer un mot de passe temporaire fourni par l'admin.
- **Écran de connexion — création de compte activée.** `Login.tsx` ne proposait auparavant que `signInWithPassword` — aucune inscription n'était possible, bug découvert en testant le flux "la personne crée son compte elle-même". Ajout d'une bascule "Se connecter"/"Créer un compte" (`supabase.auth.signUp`), gère le cas où la confirmation email est activée côté Supabase (message "vérifie ta boîte mail" + retour en mode connexion) et le cas où elle ne l'est pas (session immédiate). Le projet a une limite d'envoi d'emails de confirmation assez basse par défaut (`email rate limit exceeded` rencontré en test) — sans SMTP personnalisé configuré dans Supabase Auth, quelques inscriptions rapprochées peuvent temporairement bloquer l'auto-inscription (pas la création manuelle par l'admin, qui ne passe pas par cet envoi).
- **Incident résolu — admin privé de toutes les données à l'activation.** La quasi-totalité des policies RLS (chevaux, bobos, vaccins, photos, généalogie, pathologies, factures, etc.) reposent sur `has_role('famille'::user_role)` — une liste blanche par rôle, pas une exclusion de `'visiteur'`. En passant son propre compte en `admin`, l'utilisateur a donc perdu l'accès en lecture à tout sauf `horses`/`users` (RLS silencieuse : 0 ligne renvoyée avec statut 200, pas d'erreur — d'où l'impression de perte de données, alors qu'aucune ligne n'avait été supprimée). Corrigé en modifiant `public.has_role()` pour qu'un `admin` satisfasse aussi `has_role('famille')`, et en étendant les 3 policies `RESTRICTIVE finance_famille_only` (`expenses`/`invoices`/`invoices_staging`, basées sur `current_user_role()`) à `IN ('famille','admin')`. Côté app, `App.tsx` avait aussi son propre garde-fou `role === 'famille'` (onglet Finances + route) non couvert par `readOnly` — remplacé par `hasFamilyAccess = role === 'famille' || role === 'admin'`. Leçon retenue : tout ajout futur de valeur à l'enum `user_role` doit systématiquement vérifier les policies `has_role(...)`/`current_user_role()` existantes (liste blanche, pas liste noire) avant d'attribuer ce rôle à un compte réel.
- **Notification nouvelle inscription — pastille + email.** Deux mécanismes indépendants pour signaler à l'admin qu'un nouveau compte vient de se créer :
  - **Pastille rouge** sur la roue crantée (`App.tsx`) : au montage, si `role==='admin'`, compare la date du compte le plus récent (`admin_list_users()`, colonne `created_at` ajoutée à la fonction) à la dernière consultation de "Gestion des accès" (stockée en `localStorage`, clé `pegasus_admin_users_last_seen`, cf. `src/lib/adminNotifications.ts`). La pastille disparaît dès l'ouverture de `GestionAcces.tsx` (qui remet à jour le marqueur) ou dès le clic sur la roue crantée. Purement front, aucune donnée serveur supplémentaire ; au tout premier chargement (`localStorage` vide), aucun rattrapage rétroactif sur les comptes déjà existants.
  - **Email automatique** : fonction serverless Vercel (`api/notify-new-user.ts`, runtime Node.js — **pas** Edge/Deno, choisi précisément parce que `nodemailer` a besoin des modules réseau natifs de Node pour parler SMTP) envoyant un email via le relais SMTP Gmail (`nodemailer`, `service: 'gmail'`, mot de passe d'application — nécessite la validation en 2 étapes activée sur le compte Gmail expéditeur). Protégée par un secret partagé comparé côté serveur (header `x-webhook-secret` vs `process.env.WEBHOOK_SECRET`, comparaison `.trim()` des deux côtés pour tolérer un caractère invisible collé). Déclenchée par un trigger Postgres `AFTER INSERT ON auth.users` (`public.notify_admin_new_user()`, extension `pg_net`, `net.http_post` vers `https://pegasus-marchais.vercel.app/api/notify-new-user`). `vercel.json` exclut `/api/*` de la réécriture SPA (`"source": "/((?!api/).*)"`) pour ne pas masquer la fonction. Variables d'environnement Vercel (Production) : `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `ADMIN_EMAIL`, `WEBHOOK_SECRET` — un changement de valeur nécessite un redéploiement (bouton "Redeploy" ou nouveau `git push`) pour être pris en compte, les déploiements existants ne relisent pas les env vars à chaud. Choix Edge Function Supabase écarté : aurait ajouté un second pipeline de déploiement (CLI/dashboard Supabase) en plus de celui déjà en place sur Vercel, pour un gain nul ici.
- **Rôle Visiteur — activé en production.** Table `public.users` (déjà existante, non exploitée avant ce sprint) porte la colonne `role`, typée enum Postgres `user_role` (pas une contrainte CHECK — valeurs `'famille'`/`'groom'`/`'visiteur'`, les deux dernières ajoutées via `ALTER TYPE user_role ADD VALUE`). Nouveau hook `useUserRole` (lit le rôle de l'utilisateur connecté, fail-closed sur `'visiteur'` si la ligne est absente/erreur). Onglet Finances masqué dans `BottomNav` + route bloquée dans `App.tsx` pour tout rôle ≠ `'famille'`. RLS restrictive côté Supabase sur `invoices`/`expenses`/`invoices_staging` (fonction `public.current_user_role()`, policy `AS RESTRICTIVE` limitée à `'famille'`, s'ajoute aux policies existantes sans les remplacer). En mode visiteur (`role==='visiteur'`), boutons d'écriture masqués/désactivés : Accueil (Démarrer une visite / visite pro), Soins (Signaler un bobo, édition inline des dates de rappel vaccin, "Annuler le rappel"), Galerie (suppression photo, ajout/suppression de tags, génération de miniatures). Le rôle `'groom'` existe dans le type mais n'a aucune UI dédiée pour l'instant (module Groom = roadmap S6). Premier compte visiteur créé : Charles-Axel (`public.users.id` lié à son compte Supabase Auth, `role='visiteur'`) — la ligne `public.users` est auto-créée par un trigger à la création du compte Auth (role par défaut `'famille'`), donc les comptes visiteur/groom futurs nécessitent un `UPDATE public.users SET role=... WHERE id=...` après création du compte Auth, pas un `INSERT`

- Visite du jour / Visite pro — accordéon "Bobos actifs" : le titre du bobo n'est plus tronqué une fois l'accordéon ouvert (`truncate` retiré conditionnellement sur `isOpen`, wrap multi-ligne). La modale "Ajouter un suivi" (`VisitModal`, `BoboCard.tsx`) affiche désormais en en-tête le badge cheval + le titre/contexte du bobo (`getBoboTitle`) et un bloc "Historique (N)" dépliable listant les visites passées + l'entrée initiale d'ouverture — auparavant un formulaire nu sans aucun contexte. `VisitModalProps` accepte maintenant `horse`/`pathology` optionnels, câblés depuis les 3 points d'appel (VisiteSheet, VisiteProSheet, BoboCard lui-même).
- `QuizPlay.tsx` : une question mal répondue n'est plus exclue définitivement de la rotation comme une question réussie — elle redevient éligible après `2 * SESSION_SIZE` (20) tentatives, toutes questions confondues, ce qui approxime "2 parties" sans nécessiter de nouvelle table de suivi de session (calculé côté client à partir de `quiz_attempts.answered_at`/`is_correct`, dernière tentative par question).
- **Module Quiz** (nouvel onglet BottomNav, icône point d'interrogation, visible pour tous les rôles y compris Visiteur/Groom). QCM 4 choix, 1 bonne réponse. Deux systèmes de niveau coexistent selon le lot de questions : `niveau` (Facile=1pt/Difficile=2pts, lots pathologies/vaccins/généalogie Pegasus) ou `galop` (1 à 7, niveau Galop FFE, 1-3pts, lots de culture générale équestre) — l'un des deux est toujours `NULL` selon le lot, jamais les deux à la fois. Deux tables : `quiz_questions` (question/reponse_a-d/bonne_reponse/niveau/galop/points/categorie/thematique/lot/source_id/external_id/reported, RLS lecture ouverte à tous les connectés + UPDATE ouvert authenticated pour le bouton Signaler, pas d'INSERT via l'appli — peuplée par import SQL direct) et `quiz_attempts` (ledger append-only : user_id/question_id/chosen_reponse/is_correct/points_earned/answered_at, RLS lecture ouverte à tous — nécessaire pour les classements —, écriture restreinte à `user_id = auth.uid()`). `categorie` détermine le type de ressource pointée par `source_id` (polymorphe, résolu par `resourceTypeForCategorie` dans `quizUtils.ts`) : `Maladies`/`Parasites` → `pathologies.id` (uuid), `Vaccins` → nom de vaccin en texte libre (résolu de façon tolérante par `resolveVaccineDbType`, `'multi'` = pas de vaccin unique donc pas de lien), `Généalogie` → slug du nom de cheval (`slugify`, peut ne correspondre à aucun cheval de la base si ancêtre externe — alors pas de lien) ; les lots de culture générale (Robes/Squelette/Harnachement/Comportement/Carrière/Soins/Disciplines/CSO) n'ont pas de `source_id` du tout (pas de fiche associée). Page `Quiz.tsx` : deux podiums (Points, Taux de bonnes réponses — ce dernier nécessite un minimum de 5 réponses pour apparaître, `MIN_ATTEMPTS_FOR_ACCURACY`) calculés côté client à partir de `quiz_attempts` + `public.users`, + bouton "Jouer". `QuizPlay.tsx` : session de `SESSION_SIZE=10` questions piochées aléatoirement **parmi celles jamais posées à l'utilisateur courant** (comparaison avec son historique `quiz_attempts`, tous types confondus) — repart automatiquement sur l'ensemble complet une fois toutes posées, sans effacer les points/tentatives déjà acquis. Chaque réponse est enregistrée immédiatement dans `quiz_attempts` dès "Valider" (pas d'état "brouillon"), révèle la bonne réponse + lien "Voir la fiche" si résoluble (ouvre `FichePathologie`/`VaccinFiches`/`FicheCheval` en overlay), boutons "Terminer" (toujours) / "Question suivante" (masqué sur la dernière question). Bouton "Signaler une question mal posée" sous les 4 propositions (visible à tout moment, pas besoin d'avoir validé) : passe `reported=true` sur la question (exclue de la rotation pour tout le monde tant que non corrigée manuellement en base) et avance à la question suivante sans enregistrer de tentative ni affecter le score. Deux lots de questions fournis à ce jour : Lot 1 (250, pathologies/vaccins/généalogie Pegasus) + lot Galop complet (211, culture générale équestre, 3 sous-lots Robes-Squelette-Harnachement/Comportement-Carrière-Disciplines/Aplombs-Ferrure-Reproduction) = 461 questions au total. Script SQL unique de création des tables + RLS + import des deux lots généré et fourni à l'utilisateur (`Pegasus_Quiz_Setup.sql`, à exécuter dans le SQL Editor Supabase) — vérifier son exécution avant de supposer les tables présentes en base.

- **Module Groom — S6 livré.** Table `groom_visits` (posée dès la fondation du projet, cf. `AGENTS.md`, jamais exploitée avant ce sprint — colonnes `user_id`/`visit_date`/`amount_ttc` (défaut 7,00 €)/`paid_month`/`is_paid`/`paid_at`/`notes`, RLS déjà correcte via `has_role('famille'/'groom')`). Seul ajustement schéma : suppression de la contrainte `UNIQUE(user_id, visit_date)` (`Pegasus_Groom_Setup.sql`) pour autoriser une 2ᵉ visite le même jour sans qu'elle compte dans le nombre de jours à payer (dédoublonnage côté application : seule la visite la plus ancienne du jour, par `created_at`, est retenue dans le total). Rôle groom passé en lecture seule (`readOnly = role==='visiteur' || role==='groom'` dans `App.tsx`) sur Accueil/Soins/Galerie — Finances reste inaccessible via `hasFamilyAccess` (inchangé). Accueil (`isGroom` + `userId` en props) affiche pour ce rôle une carte "J'arrive aujourd'hui" (insert `groom_visits`, `paid_month: ''` explicite car colonne NOT NULL sans défaut) à la place du bouton "Démarrer une visite" de la Famille — pas de compteur de jours sur cette carte (retiré, redondant avec l'écran Finances côté Famille) ; "Démarrer une visite pro" reste visible pour le groom (seul bouton d'écriture de l'Accueil Famille conservé pour ce rôle, `readOnly` sinon inchangé). Finances → carte "Paiement Groom" avec badge rouge inline (nombre de mois non soldés, même style pilule que le badge bobos actifs de `HorseCard`) ouvrant `GroomSettlement.tsx` : chaque période non soldée (mois calendaire, y compris le mois en cours) affiche le nombre de jours, un montant TTC éditable (préempli jours × 7 €, somme des `amount_ttc` réels), une liste de jours dépliante (`weekdayDateFr`, "Lundi 06/07") et un bouton "À ventiler" ouvrant une popup de ventilation — **réutilise telle quelle** la logique de `LineVentilation` (répartition égale ajustable en %, correction d'arrondi automatique via `effectiveShares`), extraite de `SaisieFacture.tsx` vers `src/components/LineVentilation.tsx` (composant partagé, exporte aussi `shareSum`/`isSharesValid`) pour éviter la duplication. "Solder ce mois" écrit `invoices`+`expenses` (même pattern insert/rollback que `SaisieFacture.tsx`) puis marque les `groom_visits` du mois `is_paid=true`/`paid_month=<ym>` (ordre inverse en cas d'échec : visites marquées payées en premier, remises à `is_paid=false` si l'écriture facture échoue — évite une double-facturation au nouvel essai). **Règlement anticipé et report** : si le mois en cours est soldé avant sa fin (confirmation "Le mois n'est pas terminé, souhaitez-vous cependant solder le mois ?" requise dès que `ym >= mois calendaire actuel`), toute visite ultérieure datée dans ce même mois calendaire est reportée sur le mois suivant disponible (`effectivePeriodYm`/`groupUnsettledGroomVisits` dans `financeUtils.ts` : un mois déjà présent dans les `paid_month` historiques ne peut plus resservir, la période "déborde" sur le mois calendaire suivant jusqu'à en trouver un libre) — la carte porte alors le libellé du mois suivant même si les visites qu'elle contient sont physiquement datées dans le mois précédent.

Tables Supabase existantes : `health_events`, `health_event_visits`, `farm_alerts`, `ambiance_photos`, `photo_tags`, `vaccinations`, `vaccine_exclusions`, `veterinaires`, `marechaux`, `osteopathes`, `dentistes`, `soin_reminders`, `invoices`, `invoices_staging`, `expenses`, `quiz_questions`, `quiz_attempts`, `groom_visits` (RLS confirmé : Famille = ALL, Groom = ses propres lignes uniquement). `invoices`/`expenses` utilisées par l'écran Finances (Saisie de facture + règlement Groom) ; `invoices_staging` toujours sans code applicatif (réservée au pipeline OCR). `quiz_questions`/`quiz_attempts` créées et peuplées en production (461 questions, Lot 1 + Galop complet).

## Roadmap

- S5 (suite) : pipeline OCR factures (n8n + Claude Vision → `invoices_staging`, relecture/validation avant écriture dans `invoices`/`expenses`)
- Notifications push via Firebase FCM
- Ingestion WhatsApp pour saisie terrain (phase 2)
- Migration logique rappels vaccins depuis Excel vers Supabase/app

## Outils

GitHub (édition web historique, migration possible vers édition directe via Claude Code) · Vercel (CI/CD auto) · Supabase SQL Editor (pas de CLI Supabase) · Lucide Icons + Tabler Icons · piexifjs (EXIF) · WeasyPrint / openpyxl (génération PDF/Excel de référence).
